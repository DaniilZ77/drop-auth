apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: redis-auth-data
  name: redis-auth-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
